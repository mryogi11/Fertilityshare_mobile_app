<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/splash_fade_animation</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Splash Screen Theme using androidx.core.splashscreen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background to white -->
        <item name="windowSplashScreenBackground">#FFFFFF</item>

        <!-- Set the animated icon to display on the splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_logo_animation</item>
        <!-- Set the animation duration (optional, the AVD itself has a duration) -->
        <item name="windowSplashScreenAnimationDuration">2000</item> <!-- e.g., 2 seconds -->

        <!-- Set the theme to be applied after the splash screen is dismissed -->
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
</resources>
